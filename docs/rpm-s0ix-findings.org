#+TITLE: i915 runtime-PM + s0ix findings (2026-09-18 session)
#+STARTUP: showall

Notes for the next review round. Hardware: ThinkPad X1 Nano, TGL, NetBSD 11,
kernel built from this tree (i915drmkms builtin). Goal: sustained platform
S0ix so the EC slow-blinks the power/lid LEDs, at ~1W.

* What was added this session
- =hw.i915rpm= sysctl (kernel/external/bsd/drm2/i915drm/i915_pci_autoconf.c)
  driving new functions =i915_lispbsd_rpm_suspend/resume= in i915_drv.c.
  Values: 0=active, 1=runtime-suspend (no PCI D3), 2=runtime-suspend + PCI D3.
- The functions replicate the essential parts of the upstream (Linux)
  =intel_runtime_suspend/resume=, which are #ifndef __NetBSD__'d out because
  NetBSD never wired the pm_runtime autosuspend timer. Unlike the system-suspend
  path (i915_drm_suspend*, used by pmf/lid), they do NOT tear down the KMS
  modeset, so X SURVIVES the transition. This is the key capability the freeze
  path lacked (freeze D3'd the GPU but killed X and left display powered).

* What works
- =rpm=1= (GT parked -> RC6, display power wells released -> DC9 via
  intel_display_power_suspend, NO PCI D3): STABLE, X survives, repeatable.
- =rpm=2= (adds GPU PCI PMCSR D3): works for SHORT holds (a couple seconds,
  GPU D0->D3->D0, X survives). This is new — the freeze path could never D3
  the GPU and come back with X alive.

* What breaks (the current wall)
- Holding =rpm=2= (GPU in D3) for ~30s PANICS. Two panic signatures seen:
  1. =panic: cpuN[.. lnxsyswq]: heart stopped beating= + "coretemp0:
     workqueue busy: updates stopped" — heartbeat(9) watchdog; the shared
     Linux workqueue (lnxsyswq) got stuck holding a lock, a softint spun on
     _atomic_swap_32, watchdog fired.
  2. =panic: trap= in =tasklet_softintr= — an i915 tasklet (interrupt
     bottom-half, e.g. execlists submission) trapped, almost certainly
     faulting on GPU MMIO after PCI D3 pulled the device out from under it.
- Root cause: with the GPU PCI device in D3, MMIO is dead, but i915 still has
  live async contexts (workqueues, tasklets, timers) that touch the GPU. Linux
  prevents this with pervasive runtime-PM wakerefs (any access auto-resumes);
  we have no such guard.
- Attempted fix (already in this tree's rpm_suspend): before power-down call
  =drm_kms_helper_poll_disable= + =intel_hpd_cancel_work= +
  =intel_fbdev_set_suspend(SUSPENDED)=, and removed the erroneous
  =intel_hpd_poll_init= (which was ENABLING connector polling -> MMIO).
  Necessary but NOT sufficient: the tasklet-trap panic still happens. The
  execlists/interrupt tasklet(s) still need to be quiesced/killed before D3
  (likely intel_engine tasklet_disable/kill via a fuller GT park, or masking
  the GT interrupt source), and any pending GT retire work flushed.

* Residency data (all measured with machdep.cidle.residency = package Cx MSRs)
- Screen off (DPMS) only, GPU D0: pc9 ~= 84%, pc10 = 0%. So DPMS-off alone
  reaches pc9 but the driver still holds display power wells -> no pc10.
- =rpm=1= + all peripherals PMCSR-D3 + iwx down + LPS0=1 (full s0ix recipe,
  network dropped, 30s window): pc9 = 9%, pc10 = 0% — WORSE. Something in this
  combination keeps the package ACTIVE (pc0-2 ~91%). Open question: is it the
  (buggy) LPS0 handshake, a spinning i915 work item under rpm=1, or the
  peripheral-D3 writes? Needs isolation.
- Earlier sessions reported ~47% pc10 with GPU in plain D0 + peripherals D3 +
  LPS0 + screen off. Not reproduced cleanly this session.

* CRITICAL measurement gap (from Astra's review — agreed)
- machdep.cidle.residency reads PACKAGE C10 (MSR 0x632). It does NOT read
  platform SLP_S0 residency. The LED is gated by SLP_S0#, i.e. the PLATFORM
  transition, not package PC10. High PC10 with SLP_S0=0 => platform never
  entered S0ix => LED never blinks. This is fully consistent with everything
  observed. Next diagnostic MUST add SLP_S0 residency (LPIT system-residency
  counter, or TGL PMC), reported as before/after deltas alongside PC10, plus
  the PMC latched low-power status to name the specific blocker.

* LPS0 handshake bug (from Astra — confirmed by reading acpi.c here)
- acpi_lps0_enter/exit use =if (msft_mask) {...} else if (intel_mask) {...}=,
  so any nonzero Microsoft mask suppresses the Intel interface entirely, and
  the msft branch omits func 5 (LPS0 ENTRY) / func 6 (LPS0 EXIT). Linux issues
  functions from BOTH UUIDs, each gated on its capability bit. Patch pending
  (nanobsd-lps0-handshake.patch) — not yet applied/built/tested here.
- Also: _DSM errors are swallowed and the sysctl always reports success; the
  "entry handshake issued" log proves nothing. Patch adds per-call logging.

* s0ix command is not a real suspend (from Astra — confirmed)
- The lid s0ix path turns off display, sets device power states, issues LPS0
  notifications, then returns. It never freezes userspace or suspends
  timekeeping. A real S0 path must quiesce userspace + device activity,
  suspend periodic timekeeping, and idle CPUs deep while waiting on wake IRQs.

* Infra bug fixed this session (durable)
- NetBSD boot.cfg treats =#= as a comment char ANYWHERE on a line, so any
  =menu== whose LABEL contains =#= is truncated and DROPPED from the menu.
  This silently ate kernel entries and shifted default= indices. NEVER put =#=
  in a boot.cfg menu label. =default== is 0-indexed over the DISPLAYED list.
  Reliable index-proof boot: at the =boot>= prompt, =boot netbsd.i915.rpm=.

* Suggested next steps (for review)
1. Apply + build + hw-test the LPS0 handshake patch; capture per-function
   ACPICA status + LED behaviour with power settings otherwise unchanged.
2. Add SLP_S0 residency (LPIT/PMC) to the residency sysctl; always report
   PC10 and SLP_S0 deltas over the same quiet window.
3. Only then resume GPU-D3 work, and first solve the tasklet/workqueue
   quiescence so rpm=2 can be held (kill execlists tasklet + flush GT work +
   mask GT irq before PMCSR D3), or decide rpm=1 is sufficient once SLP_S0 is
   actually asserted.

* UPDATE 2 (2026-09-18 ~11:00): SLP_S0 measured; blocker confirmed
Added hw.acpi.sleep.slp_s0 sysctl. LPIT (phys 0x9104B000) has 3 native-Cstate
descriptors: [0] ResidencyCounter = FFH/MSR 0x632 (= package PC10);
[1] ResidencyCounter = SystemMemory MMIO 0xfe00193c, 32-bit, freq=8197 (= the
PMC SLP_S0 platform residency); [2] another. sysctl now selects the
SystemMemory (PMC) counter.

Measured deltas over quiet windows (kernel #54):
- screen-off ONLY (net up):                 pc9=83% pc10=0 SLP_S0=0
- screen-off + lps0 ONLY (net up):          pc9=83% pc10=0 SLP_S0=0
- screen-off + rpm=1 + all-periph-D3 +
  iwx-down + lps0 (net down, 30s):          pc9~0 pc10=0 SLP_S0=0

Conclusions (now measured, not hypothesised):
1. SLP_S0 platform residency is 0 in EVERY configuration -> the platform never
   enters S0ix. The corrected LPS0 handshake (all _DSM funcs AE_OK) is
   necessary but NOT sufficient. This is why the LED never slow-blinks.
2. PC10 is also 0 everywhere; best deep state reachable is PC9 (~83%) via plain
   screen-off. The periodic tick + background activity cap it at PC9.
3. rpm=1 + peripheral-D3 recipe is COUNTERPRODUCTIVE: pc9 83% -> ~0. Confirms
   Astra: intel_gt_runtime_suspend is a stub (no real GT idle) so it leaves
   activity; and/or the peripheral-D3 churn. Do NOT use this recipe.

* QUESTIONS FOR NEXT ROUND (S0-idle suspend transition on NetBSD)
The demonstrable blocker is: no real S0-idle suspend path. NetBSD has no Linux
s2idle framework. To reach SLP_S0 we need, while the machine is "frozen":
  (a) freeze/quiesce userspace (no user threads running),
  (b) suspend periodic timekeeping / the hardclock tick (so CPUs aren't woken
      100x/s) and switch to a single wake source,
  (c) REAL GT idle (intel_gt_suspend_prepare/wait_for_suspend, not the stub),
      honoring the PM_SUSPEND_TO_IDLE exemption in intel_gt_suspend_late,
  (d) all CPUs in the deepest mwait (C10) waiting only on wake IRQs,
  (e) then the SoC/PMC can gate to SLP_S0 and the EC slow-blinks the LED.
Specific asks:
  - Best NetBSD primitive to freeze userspace for s2idle? (there is an existing
    acpi_enter_freeze that kpause-loops but does NOT freeze userspace or stop
    timekeeping — is extending it the right path, or a dedicated cpu_idle deep
    loop across all CPUs?)
  - How to suspend the hardclock/timecounter tick cleanly for the freeze window
    on NetBSD (tickless cidle exists in modules/cidle) and resume TSC-corrected?
  - Minimal correct GT quiesce sequence to call from a NetBSD s0-idle path
    (wait_for_suspend + which of the suspend_late steps to KEEP vs skip so X
    survives), given the fake pm_runtime wakerefs.
  - Is holding GPU in PCI D3 even required for SLP_S0, or does real GT RC6 +
    display DC9 suffice (given rpm=2 PCI-D3 races/faults without wakerefs)?
Measurement is now in place (hw.acpi.sleep.slp_s0), so any proposed transition
can be validated by SLP_S0 delta over a quiet window.

* UPDATE 3 (2026-09-18 ~11:20): `ifconfig iwx0 down` SPINS THE CPU
Isolated cleanly (downed iwx while staying connected over USB ethernet): with
iwx0 DOWN and nothing else changed, core C7 residency = 0% and package pc6..pc9
all 0% over 15s — i.e. a CPU is busy-spinning 100%. The NetBSD iwx(4) driver's
stop/down path spins. This CORRECTS earlier attribution: the pc9 83%->~0
regression in the "full recipe" was iwx0 down, NOT rpm=1 or peripheral-D3.
Implications:
- NEVER use `ifconfig iwx0 down` in a power/s2idle path; it is actively harmful.
- Quiesce WiFi some other way for s2idle: PMCSR D3 the iwx PCI device directly
  (pcictl config write) or a proper pmf_device_suspend, WITHOUT the ifconfig
  down-path spin. (The old pc10max recipe used iwx0 down; suspect its ~47% pc10
  was measured on a kernel where iwx behaved differently, or was intermittent.)
- Current clean baselines: screen-off + net-up(powersave) = pc9~83%, pc10=0,
  SLP_S0=0. Best reachable idle is still only PC9; PC10/SLP_S0 need the full
  s2idle transition (freeze userspace + suspend tick + CLEAN network quiesce).

* UPDATE 4 (2026-09-18 ~11:30): crude userspace-freeze ALSO spins -> dead end
Tested SIGSTOP of the desktop (pkill -STOP -u mag) + screen-off, network UP
(no ifconfig down): core C7 -> 0%, pc8/9/10 = 0, SLP_S0 = 0. So freezing the
compositor makes the (unstopped, likely root) Xorg busy-spin waiting on the
frozen client. Combined with UPDATE 3 (iwx0 down spins), the conclusion is:
  *** Every crude quiesce method triggers a CPU busy-spin and makes idle WORSE.
      The best reachable state is plain screen-off with everything running
      normally: pc9~83%, pc10=0, SLP_S0=0. ***
Also: suspendsched() is NOT reversible (sets SSTOP + L_WREBOOT for reboot; no
resumesched), so the Linux-freezer approach is unavailable as-is.

Therefore reaching PC10/SLP_S0 requires a PROPER, coordinated s2idle transition
that quiesces cleanly without triggering these driver/X busy-waits:
  - a reversible userspace freeze (needs building; suspendsched is one-way),
  - clean device quiesce via pmf_device_suspend (NOT ifconfig down / SIGSTOP),
    and X handed off cleanly (wsdisplay_handlex already exists in the freeze),
  - per-CPU tick suspend during the window (tickless cidle is the basis),
  - a single wake source (lid/power GPE) + a one-shot-timer safety timeout.
This is a real NetBSD s2idle framework, not a tweak. Measurement
(hw.acpi.sleep.slp_s0) is in place to validate any attempt. Holding for
architectural guidance before building it on the daily machine.

* UPDATE 5 (2026-09-18 ~12:00): STAGE 1 DONE — reversible freezer works
Implemented Astra's stage-1 reversible freezer in acpi.c:
- acpi_s0_freeze_userspace(bool): iterate allproc, skip PK_SYSTEM + curlwp,
  lwp_suspend()/lwp_continue() each user LWP (mirrors suspendsched locking but
  reversible). Coordinator acpi_s0_freeze_test() runs in the PK_SYSTEM s2idle
  kthread: handlex(0) -> freeze -> kpause(1s settle) -> snapshot -> kpause(10s
  hold, ticks ON) -> snapshot -> thaw -> handlex(1) -> log deltas.
- sysctl hw.acpi.sleep.s0freeze=1 triggers it (mode flag on the freeze thread).
RESULT (kernel #55): clean freeze/thaw, X SURVIVES, zero stuck procs, my ssh
session stalled ~11s then resumed. Mechanism validated.
BUT residency during the 10s hold: pc9=0 pc10=0 slp_s0=0 (WORSE than screen-off
83%). Hypothesis: handlex(0) hands the panel to the wsdisplay console which
re-enables scanout (display active) -> package can't idle. This is precisely
what stage 2 (i915 S0 display-off lifecycle) must fix: turn the display off
(DC9) in-kernel during the frozen window. Need to confirm it's display-scanout
and not a kernel busy-spin (didn't log core C7 this run).
NEXT: stage 2 — call the i915 system-suspend lifecycle (drm_suspend_ioctl,
i915_drm_prepare/suspend/suspend_late) with an S0-idle target inside the frozen
window, then resume-restore before handlex(1). Measure residency again.

* UPDATE 6 (2026-09-18 ~12:20): BREAKTHROUGH — freezer+display-off => pc9=93%
Made handlex conditional (s0freeze=2 skips it) and added core-C7 logging.
Test: userland `xset dpms force off` (panel off, X alive) then s0freeze=2
(freeze userspace, NO handlex so console doesn't re-light the panel, ticks ON):
  RESULT (kernel #56, 10s hold): c7core=99%  pc9=93%  pc10=0  slp_s0=0.
Interpretation:
- The freezer is clean (c7=99%, no busy-spin). Earlier pc9=0 WAS the handlex->
  console->panel-on path; skipping handlex + DPMS-off fixes it.
- Cores 99% idle, package 93% in PC9 -- best state yet, X survives, reversible.
- pc10 STILL 0. With cores fully idle and display off, the ONLY remaining
  wakeup is the 100Hz LAPIC periodic tick (hardclock every 10ms) -> the package
  cannot hold CC10 long enough for PC10. This precisely confirms Astra stage 3:
  tick suppression is the specific PC10 blocker (now demonstrated, not assumed).
NEXT = stage 3: slow or suspend the LAPIC periodic tick during the frozen
window. Open question for Astra: on TGL does the LAPIC timer deadline still
wake a core from CC10 (normal tickless idle relies on this), so simply LOWERING
the tick rate (e.g. reprogram LAPIC initial count to ~2Hz) during the hold
would open >100ms idle gaps and reach PC10 with NO hardware-wake-from-C10 risk
and kpause still functioning -- vs. fully masking LVT_TIMER (Astra's approach)
which needs an external wake (RTC/HPET) that survives C10 and risks a hard hang
if the wake fails. Prefer the low-risk slow-the-tick variant for the first PC10
test. Timekeeping: TSC timecounter (q=3000) is tick-independent so wall clock is
fine; only callout latency grows during the short window.

* UPDATE 7 (2026-09-18 ~12:35): correction — cidle IS tickless; PC10 blocker is callouts
IMPORTANT: the cidle.c in the repo was STALE (6.5KB). The BUILT module on the
machine (now synced, 13KB) IS tickless: cidle_idle_tickless() calls
lapic_oneshot(skip)/lapic_oneshot_done() to STOP the periodic tick and sleep to
the next callout, capped at maxskip=100 ticks (1s), then replays elapsed
hardclock(). So Astra UPDATE-round assumption "cidle never changes timer
programming" was based on the stale file -- please re-read the synced cidle.c.
Runtime tlstats on #56: entries=199245 ticks_skipped=248508 deep=3974
woke_early=195271. So tickless works but MOST idle periods WOKE EARLY -- the
CPU is woken before its skip target by callouts/interrupts. cidle only skips to
callout_next_ticks(), so the real PC10 cap is the NEAREST CALLOUT, not the raw
100Hz tick. Even with userspace frozen + display off (pc9=93%), kernel callouts
(scheduler, softclock, watchdog, TCP, etc.) keep firing -> gaps too short for
PC10 (PC10 entry latency > the gap).
=> Stage 3 is really "quiesce callouts AND the tick during the frozen window",
not just mask the tick. This matches Astra's note to "handle callout and
watchdog accounting explicitly." The freezer (stage1) suspends user LWPs but
NOT kernel callouts. Options to explore with Astra:
  - raise cidle maxskip and quiesce/defer non-essential callouts during freeze,
  - or a dedicated s2idle idle path that masks LVT_TIMER + defers the callout
    wheel + a hardware wake (RTC/HPET) + kern_tc.c TSC-wrap handling on resume.
State banked: stages 1+2 reach pc9=93% cleanly (freezer+display-off, X
survives, reversible). PC10/SLP_S0/LED remain, gated by callout+tick quiescence
(delicate timekeeping work — recommend Astra guidance before building).

* UPDATE 8 (2026-09-18 ~12:45): confirmed PC10 cap = callouts (zero-risk probe)
Tried raising cidle maxskip to 2000 during a frozen hold: REJECTED (code-caps
maxskip at 100). Ran s0freeze=2 anyway: pc9=94% pc10=0, tlstats woke_early=266k
vs deep=5.7k. So even the 1s skip cap is never reached -- callouts wake each CPU
within a few ms. CONCLUSION (zero-risk, definitive): the pc9->pc10 cap is the
CALLOUT WHEEL (per-CPU scheduler/softclock/etc.), not the raw tick nor maxskip.
Stage 3 must DEFER/QUIESCE callouts (and the tick) across the frozen window and
fix timekeeping/watchdog on resume -- delicate, needs the kern_tc.c/callout
handling Astra scoped. This is the last mile to PC10/SLP_S0/LED; recommend Astra
guidance before implementing (hard-hang/timekeeping risk on the daily machine).

* UPDATE 9 (2026-09-18 ~13:00): goal part (1) "WiFi/TB in D3 + ASPM L1.2" — mostly N/A here
Validated the PCIe topology/ASPM directly (safe config reads, ethernet-connected):
- WiFi = 00:20:3 Intel AX201, but it is CNVi (integrated), NOT a PCIe endpoint:
  it has NO PCIe Link Control / ASPM registers at all (only a PMCSR D-state).
  So "WiFi link in ASPM L1.2" is not applicable to this WiFi.
- Thunderbolt = 00:07:0 / 00:07:2 PCIe root ports: ASPM L1 already enabled
  (Active State PM Control = L1 Entry Enabled), BUT LTR Mechanism Enabled = OFF
  on the port, and LTR is a hard prerequisite for ASPM L1.2 -> L1.2 cannot
  engage. Also both TB links are EMPTY (nothing plugged in) so their ASPM state
  is moot for power. TB DMA/USB4 HCI = 00:13:2 / 00:13:3.
- NVMe PCIe link already has ASPM L1.2 enabled (endpoint L1SS control 0x4069000f,
  from earlier). That is the one PCIe link where L1.2 is real and active.
- All these devices CAN be forced to PMCSR D3 (pcictl config write) and that was
  done in earlier peripheral-D3 tests; it did not move PC10 (callout wheel caps).
CONCLUSION: goal part (1) as worded ("WiFi/TB in D3 with the link in ASPM L1.2")
came from a Linux/desktop-PCIe mental model. On this TGL laptop WiFi is CNVi (no
ASPM link) and the TB links are empty with LTR off, so ASPM-L1.2 on those links
is N/A / moot. The real power gate is PLATFORM S0ix (SLP_S0), which needs the
s2idle transition (stages 1-3), NOT device-link ASPM. Device D3 alone (incl.
WiFi/TB) does not produce PC10/SLP_S0/LED here -- established across many tests.

* UPDATE 10 (2026-09-18 ~13:15): pc10 blocker isolated by ELIMINATION = tick/callout only
Ran freeze(stage1) + display DC9 (hw.i915rpm=1, intel_display_power_suspend) +
WiFi PMCSR-D3 + userspace frozen, all together (bounded, auto-thaw, X survives):
  RESULT: c7core=99% pc9=94% pc10=0 slp_s0=0.
Also freeze + WiFi-D3 alone (display DPMS-off): pc9=95% pc10=0.
So with EVERY other variable quiesced -- userspace frozen, display in DC9 (wells
off, not just DPMS), GT parked, GPU/WiFi handled, cores 99% in C7 -- the package
still tops out at PC9 and never PC10. This ELIMINATES display state, device
D-states, and idle depth as the pc9->pc10 gate. The ONLY remaining variable is
the frequency of brief wakes (the ~ms-scale LAPIC tick + callout activity):
PC9 commits with its short entry latency; PC10's longer entry latency never
elapses between the frequent wakes. => Stage 3 (reduce/suspend the tick+callout
wakes during the frozen window) is now the SOLE remaining blocker, proven by
elimination, for PC10 -> SLP_S0 -> LED.
Note on hang-safety for stage 3: the LAPIC timer-deadline wake already fires the
cores out of PC9 every cycle; the same armed-deadline mechanism wakes a core
from PC10 too (standard tickless contract), so LOWERING the effective wake rate
(fewer callouts / slower tick) should let PC10 commit while STILL waking at the
deadline -- i.e. a slow-the-wakes approach need not risk a no-wake hang, unlike
fully masking LVT_TIMER. The delicacy is callout-wheel quiescence + heartbeat +
timekeeping accounting. Recommend Astra design; ready to implement + measure.

* UPDATE 11 (2026-09-18 ~13:30): iwx can't be cleanly quiesced; network-quiesce backfires
Tried the definitive network-vs-tick isolation: detached script quiesced ALL
network (WiFi PMCSR-D3 + xhci D3) + display DC9 + freeze userspace, measured via
kernel log (no live connection). RESULT: c7core=99% pc9=66% (DOWN from 94%!)
pc10=0. WiFi PMCSR-D3-while-driver-up makes the fragile iwx driver busy-spin
(same failure class as `ifconfig iwx0 down` in UPDATE 3, and the GPU-D3 MMIO
fault) -> pc9 drops. So EVERY power-down path for iwx (ifconfig down, PMCSR D3)
spins the CPU. Cannot cleanly remove WiFi interrupts this way.
=> Two hard walls remain, both requiring careful driver-level suspend, not
config pokes:
  1. iwx WiFi: needs a proper pmf_device_suspend (driver .suspend hook), which
     the freeze keep-list currently AVOIDS (keeps iwx up) because iwx is fragile
     (also panics on reboot). A clean iwx suspend/resume is prerequisite to
     removing WiFi wakeups.
  2. callout/tick quiescence (stage 3) for the residual periodic wakes.
Clean ceiling remains pc9=94% (network up, everything else quiesced, cores 99%
idle). PC10/SLP_S0/LED need BOTH walls solved via proper device-suspend +
s2idle callout/tick handling -- delicate, driver-level, recommend Astra design.
This session established the complete, measured map of every blocker.

* UPDATE 12 (2026-09-18 ~14:00): corrections per Astra review of 325f8fc/a8c55c3
Astra's review was right; correcting earlier over-claims:
- "callouts are DEFINITIVELY the pc10 blocker" is NOT established. The
  woke_early counter records lapic_oneshot_done()'s pending-timer flag, NOT the
  wake source or idle duration. Early wakes could be device IRQs, IPIs, or
  monitor-address activity. The skip<minskip path increments no counter, and
  tlstats aggregates across CPUs and outside the frozen window. So the cause of
  pc9->pc10 failure is NOT yet identified -- it is one of {short callout
  deadlines, early interrupts, or long-sleeps-that-still-miss-PC10}.
- maxskip limit is 1400 (default 100), NOT code-capped at 100. My write of 2000
  was rejected for exceeding 1400; it did not prove a 100 ceiling. maxskip is
  settable up to 1400 (~14s at hz=100).
- SLP_S0 freeze-test measurement was BUGGY (never called acpi_slp_s0_probe, and
  ignored read errors -> could print false slp_s0=0 on a fresh boot). FIXED in
  #57 (probe first, check both reads, report ok/UNAVAIL + raw a/b + wrap-safe
  delta). RE-MEASURED freeze+DC9 on #57: slp_s0=ok a=0 b=0 d=0 -> SLP_S0 is
  GENUINELY 0 (not a false zero). pc9=94% pc10=0 stand.
- c7core=99% is UNRELIABLE: coordinator isn't CPU-bound, so the core-C7 MSR
  (0x3fe, per-core) reads may hit different cores around kpause. (pc9/pc10 use
  PACKAGE MSRs 0x631/0x632 which are core-independent, so those remain valid.)
- The committed freeze test does NOT run any i915 suspend lifecycle / GT-idle /
  LPS0 inside it; external DPMS-off + freezer is a baseline only. "stages 1+2"
  overstated it. PC9 alone does not rule out hardware package-state constraints.
NEXT (Astra's plan): instrument DPMS-off+s0freeze with per-CPU, in-window data:
requested skip (incl skip<minskip), full-width TSC around each MWAIT (idle
duration distribution), pending interrupt vectors + ci_want_resched right after
MWAIT, interrupt-count deltas over the hold, and consistently-sampled/validated
PMC + core counters. Print after thaw. This will actually identify the wake
sources and separate short-sleeps vs early-wakes vs long-sleeps-missing-PC10 --
BEFORE any timekeeping/callout-wheel surgery. Helper files (lapic.c,
kern_timeout.c, kern_heartbeat.c, kern_clock.c) now pushed for review.
Also TODO (Astra): freezer must track which LWPs it suspended (currently
lwp_continue's ALL user LWPs on thaw, incl. pre-existing stops) and replace the
fixed 1s settle with acknowledged quiescence.

* UPDATE 13 (2026-09-18 ~14:20): zero-risk wake-source probes (userland)
Mapped interrupts (intrctl): msix0=nvme0 (per-CPU ioqueues), msix3=iwx0,
msi2=xhci1, msi5=i915drmkms0, msi1=xhci0, msi4=hdaudio0.
Measured over screen-off idle (clean vmstat -i delta, after I/O settled):
~114 interrupts/s TOTAL across all CPUs (~14/s per CPU). Disk I/O = 0 during
genuine idle (iostat; the initial 32 t/s was just the dpms command settling).
(An earlier vmstat run showed ~900/s on nvme msix0 but that window caught a
transient I/O burst / fragile parse -- the clean idle rate is ~114/s total.)
INTERPRETATION: at ~14 IRQ/s per CPU, interrupts alone give ~70ms gaps -- that
should be ample for PC10 entry, yet PC10=0. So the PC10 block is NOT simply "too
many interrupts." It is either (a) the callout wheel capping cidle's skip to a
few ms (tickless skips only to callout_next_ticks(), independent of the IRQ
rate), or (b) a hardware package-state condition for PC10 not met even when
idle. Userland cannot distinguish these -- confirms Astra: need the in-kernel
idle-duration instrumentation (per-CPU: requested skip incl skip<minskip,
full-width TSC around each MWAIT, pending vectors after MWAIT) to see the actual
sleep-gap distribution and whether long sleeps still miss PC10. That is the
next build (LOW risk -- logging only, no behavior change).

* UPDATE 14 (2026-09-18 ~14:40): iwx suspend is CLEAN (correcting U11); full elimination
CORRECTION to UPDATE 11: iwx does NOT continuously spin. `drvctl -S iwx0`
(pmf_device_suspend -> pmf_class_network_suspend -> if_stop -> iwx_stop ->
iwx_stop_locked, per Astra) COMPLETES cleanly (exit 0), drives iwx to D3, and
iwx interrupts drop to 0. With iwx suspended, top/vmstat show 99.8% IDLE, no
spinning process. The earlier c7=0/pc9=0 "spin" (U11, and the ifconfig-down
result U3) was a MEASUREMENT ARTIFACT of the shell-based residency delta taken
immediately after a device power change -- NOT a real spin. (ifconfig-down may
still differ, but the clean path is drvctl -S / pmf.)
So the clean WiFi-quiesce path EXISTS (drvctl -S iwx0), contradicting U3/U11.
Re-ran the full clean quiesce via #57's fixed coordinator (all SLP_S0 reads
verified "ok", not false zeros):
  freeze + DC9 + clean-iwx-D3:                    pc9=95% pc10=0 slp_s0=0
  freeze + DC9 + clean-iwx-D3 + LPS0 handshake:   pc9=95% pc10=0 slp_s0=0
So with userspace frozen, display DC9, GT parked, iwx cleanly D3 (0 IRQ),
cores 99% idle (top-verified), AND the LPS0 entry handshake issued -> STILL
pc9=95%, PC10=0, SLP_S0=0. Every device/userspace/display/network variable is
now eliminated with trustworthy measurement.
REMAINING two candidates for the PC9->PC10/SLP_S0 gate:
  (a) callout wheel: idle gaps shorter than PC10 entry latency (cores idle in
      short bursts capped by kernel callouts). Needs in-kernel per-CPU idle-
      duration instrumentation to confirm (Astra's plan; LOW risk).
  (b) hardware/LPI coupling: the GPU is still in PCI D0 in ALL these tests
      (rpm=1/DC9 does display-power-suspend + GT-park but NOT PCI D3). The LPI
      _DSM constraints require GFX0=D3 for the SLP_S0 platform commit. If PC10
      is coupled to SLP_S0 here, it cannot assert with GPU in D0. GPU PCI-D3
      (rpm=2) faults without proper GT quiescence (intel_gt_suspend_prepare) --
      Astra's i915 S0 lifecycle work.
NEXT (two low/medium-risk builds, both measure-only-first):
  1. in-kernel idle-duration instrumentation in cidle (LOW risk) -> resolves (a).
  2. proper i915 S0 lifecycle (i915_drm_prepare/suspend/suspend_late with S0
     target + intel_gt_suspend_prepare) so GPU reaches D3 cleanly -> tests (b).

* UPDATE 15 (2026-09-18 ~14:55): idle-gap depth evidence (existing tlstats delta)
Clean tlstats delta over 15s screen-off idle (NOT frozen; zero-risk, no build):
  entries=7871 ticks_skipped=11436 deep=174 woke_early=7697
  => avg requested skip ~1.45 ticks (~14ms); deep(long) skips only 2%;
     98% of idle entries WOKE EARLY (before their skip target).
Aggregate ~525 idle-entries/s across 8 CPUs vs only ~114 IRQ/s -> most wakes are
NOT hardware interrupts. Candidates for the early wakes: callout_next_ticks()
capping the requested skip to ~14ms, AND MWAIT monitor-address writes /
scheduler activity waking even earlier (this run had userspace running). This
SUPPORTS candidate (a) short-idle-gaps as the PC9->PC10 gate, but per Astra is
not proof: woke_early does not identify the source, and the frozen case (which
still gave pc9=95%/pc10=0) removes userspace but not kernel callouts/threads.
Net after this session's full sweep: PC10/SLP_S0 gated by EITHER (a) short idle
gaps (callout + monitor-write wakes; ~14ms avg, need <PC10-entry-latency) OR
(b) GFX0=D3 hardware/LPI coupling (GPU still D0 in all tests). Both remaining
resolutions require a build:
  1. in-kernel per-CPU MWAIT duration + wake-source (pending vectors,
     ci_want_resched) instrumentation in cidle -> proves/refutes (a). LOW risk.
  2. proper i915 S0 lifecycle (intel_gt_suspend_prepare + GPU D3) -> tests (b).
     MEDIUM risk (GT quiescence; prior naive GPU-D3 faulted).
Everything device/userspace/display-side is exhausted and measured; the finish
line needs one of these two instrumented builds. iwx suspend is CLEAN (U14).

* UPDATE 16 (2026-09-18 ~15:10): CANDIDATE (b) TESTED — GPU-D3 PINS THE PACKAGE
Ran the culmination via existing clean pmf paths (no build): drvctl -S iwx0
(WiFi->D3, clean) + drvctl -S i915drmkms0 (FULL i915 lifecycle: i915_drm_prepare
-> intel_gt_suspend_prepare -> suspend -> suspend_late -> PCI D3) + LPS0 entry
handshake + s0freeze=2 (freeze userspace). X SURVIVED, GPU cleanly restored.
RESULT: c7core=99% but pc9=0% pc10=0 slp_s0=0 (verified ok).
=> With the GPU in D3 via the current i915 suspend, cores stay 99% idle (C7) but
the PACKAGE cannot even reach PC9. Since cores ARE idle, the pin is on the
UNCORE / system-agent side: the NetBSD TGL i915 suspend leaves display
PHYs/PLLs/power-wells (SA-side) powered even in device-D3. This is the SAME wall
identified at the project's start, now PROVEN by measurement.
THE CATCH-22 (root of the whole goal):
  - GPU D0 + DC9 (rpm=1): package reaches pc9=95%, but GFX0 != D3 so the LPI/
    SLP_S0 platform commit is not satisfied -> no SLP_S0/PC10.
  - GPU D3 (i915 suspend): GFX0=D3 satisfied, but the immature i915 suspend
    leaves the SA/display power up -> package pinned at pc0-8, pc9=0 -> no PC10.
Neither path yields PC10/SLP_S0. The finish line requires a FIXED i915 TGL
suspend that puts the GPU in D3 AND cleanly powers down the display SA
(PHYs/PLLs/power-wells) so the package can still reach deep C-states -- i.e.
extend intel_display_power_suspend_late / the icl display-core uninit + DC9 path
so the SA is actually gated in the S0-idle case. That is deep i915 driver work
(the original immaturity wall), not a config/quiesce tweak.
This session's net: every non-i915 blocker eliminated and measured; the
remaining, decisive blocker is the i915 SA power-down in the suspend path.
Candidate (a) short-gaps may also contribute but is moot while (b) pins pc9=0.

* UPDATE 17 (2026-09-18 ~15:30): DC9 IS armed; SA pin is deeper (power-well/entry)
Instrumented intel_display_power_suspend_late() to log DC_STATE_EN around
bxt_enable_dc9() (kernel #58). Via drvctl -S i915drmkms0 (full clean lifecycle):
  DC_STATE_EN before=0x00000000, after=0x00000008  (bit3 = DC9 enabled/armed).
So the display-power-suspend path DOES arm DC9 correctly. GPU reaches PCI D3, X
survives. Yet the package pins at pc9=0 with GPU-D3 (UPDATE 16). Therefore the
SA pin is NOT "DC9 never armed". It is one of:
  - DC9 armed but NOT ENTERED: the DMC can only transition to DC9 when the
    display is fully powered down; a residual display power well (PW1/PW2/the
    combo-PHY) or the CDCLK/DPLL still held would block the actual DC9 entry
    and keep the SA clocked. intel_power_domains_suspend() (called just before
    suspend_late) is responsible for releasing those wells.
  - or a non-display SA/GT pin from the D3 transition (PCIe link not in L1,
    GT/ring not fully gated).
NEXT diagnostic (for Astra / next build): log the actual DC-state STATUS (did
DC9 enter?) and the power-well status registers (HSW_PWR_WELL_CTL*/the ICL
PWR_WELL_CTL_AUX etc.) at suspend, while the GPU is still D0. That pinpoints
which well/PLL is stuck. This is deep i915 display-power-domain work -- the
precise, now-narrowed target for reaching PC10/SLP_S0/LED. Instrumented file
(intel_display_power.c) pushed.

* UPDATE 18 (2026-09-18 ~15:50): tickless=1 vs =0 -> gap length sets package depth
Astra's requested test (DPMS-off + freezer + DC9, identical settings, TSC-based
pc% so valid despite hardclock drift):
  tickless=1: c7core~99% pc9=95% pc10=0
  tickless=0: c7core~97% pc9=0%  pc10=0   (100Hz periodic tick running)
=> Tick suppression is ESSENTIAL: with the normal 10ms tick the package cannot
reach even PC9 (0%); suppressing the tick (gaps ~14ms) gives PC9=95%. Package
C-state depth is clearly IDLE-GAP-LENGTH limited. Progression: 10ms gap->PC9=0;
~14ms gap->PC9=95%; PC10 needs a longer gap than ~14ms (its entry latency).
The ~14ms cap is callout_next_ticks() (nearest kernel callout), NOT maxskip.
So reaching PC10 requires EXTENDING idle gaps by deferring the kernel callout
wheel during the frozen window (stage 3), with Astra's accounting fixes:
  (1) lapic_oneshot_done: preserve fractional elapsed + per-CPU clock epoch;
      program remaining-to-deadline, not a fresh full period.
  (2) entry protocol for an already-pending tick; compute elapsed independent
      of IRR; retain the actual (unclamped) programmed count.
  (3) callout_next_ticks/schedule: publish armed deadline under interlock +
      notify target CPU on earlier remote callout; distinguish cc_todo future
      entries from actually-due callbacks.
OPEN: whether long-enough gaps alone reach PC10 (candidate a) OR PC10 also needs
GFX0=D3 (candidate b) -- but GPU-D3 PINS the package (U16), so if (b) holds with
the current i915, PC10 is blocked regardless of gaps. Test after stage 3: extend
gaps with GPU-D0+DC9 and see if PC10 appears.
Correction to U13 (per Astra): 114 IRQ/s / 8 CPUs does NOT imply 70ms package-
wide quiet windows; package residency needs OVERLAPPING per-CPU idle, which an
average doesn't show.
Next build = stage 3 (callout-wheel deferral + the 3 timekeeping fixes) -- deep,
delicate; Astra has now specified the exact bugs. Then re-measure PC10.

* UPDATE 19 (2026-09-18 ~16:30): ASTRA WAS RIGHT — RC6-disable pinned pc9, not display
Implemented Astra's S0-idle GT exemption: new global i915_lispbsd_s0idle
(sysctl hw.i915s0idle), suspend_to_idle() honors it, and intel_gt_suspend_late()
takes an early return (preserving RC6/RPS/LLC, skipping gt_sanitize) when set --
with logging of rc6.enabled + GEN9_PG_ENABLE + GEN6_RC_CONTROL while in D0.
TEST (hw.i915s0idle=1 + drvctl -S iwx0 + drvctl -S i915drmkms0[GPU->D3] +
LPS0 + s0freeze):
  log: gt_suspend_late S0-idle: rc6.enabled=1 PG_ENABLE=0x00000007
       RC_CONTROL=0x88040000  (RC6/PG PRESERVED, branch ran)
  s0freeze done: c7core~99% pc9=93% pc10=0 slp_s0=0
=> With RC6 PRESERVED, GPU-D3 NO LONGER PINS the package: pc9 recovered 0%->93%.
UPDATE 16's "GPU-D3 pins via display SA" was CONFOUNDED by intel_rc6_disable()
in the non-exempt GT suspend path (Astra's diagnosis exactly). X survived.
DECISIVE CONSEQUENCE: we now hold the deepest device state possible -- GPU D3
(GFX0=D3 SATISFIED) + WiFi D3 + DC9 + RC6 on + LPS0 handshake + userspace frozen
+ cores 99% idle + pc9=93% -- and PC10/SLP_S0 are STILL 0. Therefore GFX0=D3 is
NOT the PC10 gate: CANDIDATE (b) REFUTED. The SOLE remaining blocker is
CANDIDATE (a): idle gaps too short (~14ms, callout_next_ticks-capped) < PC10
entry latency. Stage 3 (defer the callout wheel to extend gaps, + Astra's 3
lapic_oneshot/callout timekeeping fixes) is now the single, unambiguous path to
PC10 -> SLP_S0 -> LED. Every device/GPU/RC6/display/userspace prerequisite is
now achievable and yields pc9=93%.

* UPDATE 20 (2026-09-18 ~17:30): PMC requirements vs status — SLP_S0 needs BROAD IP quiescence
Added PMC diagnostics: hw.acpi.sleep.pmcreqs dumps _DSM func1 requirements (UUID
57a6512e-3979-4e9d-9708-ff13b2508972 on the LPS0 handle); s0freeze coordinator
reads the 6 PMC LPM live-status words (0xfe001c5c) mid-freeze. Enabled modes
(0xfe001c78)=0x9 => modes 0 and 3. Per-mode residency (0xfe001c80..)=ALL ZERO.

REQUIREMENTS (_DSM func1), mode0 & mode3:
  mode0 req[0..5]: 757c0236 807c01ef 0000112c 00000000 183fe805 aaa00000
  mode3 req[18..23]: f7fe027e e87c01ef 0000112d 00000000 383fe8c5 aaa00000
DEEP-STATE STATUS (iwx D3 + GPU D3 + LPS0 + userspace frozen + cores 99% idle,
captured mid-freeze; pc9=97% pc10=0 slp_s0=0):
  25580074 803c63eb 00001eea 00001040 39375a2c 000aaaaa
  (vs screen-on 25580074 803c63eb 00001eea 00001040 39b71a2c 000aaaaa -- only
   word4 differs: quiescing barely moves the PMC status!)
UNMET mode0 (req & ~status): w0=0x50240202 w1=0x00400004 w2=0x00000104 w3=0
  w4=0x0008a001 w5=0xaaa00000  (5 of 6 words still unmet in our deepest state).
CONCLUSION: SLP_S0/PC10 is NOT a single blocker (tick/callout/GPU). The PMC
requires a BROAD set of IP/PLL/clock/device-D3 conditions per mode; most stay
UNMET regardless of GPU/WiFi/display/CPU quiescing because NetBSD does not
power-manage those IPs (audio/ISH/sensors/camera/CNVi/fabric+PLL etc.). word5
0xaaa00000 unmet = per-IP power-gate reqs; word0/1/4 = PLL/clock/fabric.
NEXT: decode these unmet bits vs Linux's TGL status maps (tgl_lpm0..5 in
intel_pmc_core.c) to NAME each unmet condition, then triage addressable (pmf
device suspend) vs structural (no NetBSD PM). This is the true, quantified
blocker set. Requirement/status numbers handed to Astra for decode.

* UPDATE 21 (2026-09-18 ~18:00): *** PC10 = 52% ACHIEVED — xHCI was the blocker ***
Decoded UPDATE 20's unmet bits via Linux tgl status maps (fetched tgl.c):
word0=clocksource, w1=power_gating, w2=d3, w3=vnn_req, w4=vnn_misc, w5=signal.
Unmet mode0 named: USB2PLL_OFF_STS(w0.18), OPIOPLL_OFF_STS(w0.21),
xHCI_PG_STS(w1.2), LPSS_PG_STS(w1.22), xHCI0_D3_STS(w2.2), IS_D3_STS(w2.8),
CPU_C10_REQ_STS_0(w4.0), Int_Timer_SS_Wake0-5(w5). The prior deep test had NOT
suspended xHCI. Added drvctl -S xhci0/xhci1 (safe now: on WiFi, USB-eth gone):
  RESULT: c7core~99% pc9=0% PC10=52% slp_s0=0
  PMC status 25580074 803c63ef 00001eee 00001040 3d375a2c 000aaaaa
  -> xHCI_PG_STS (w1.2) and xHCI0_D3_STS (w2.2) now SET. Residency moved
     pc9->PC10=52%. *** FIRST non-zero PC10 in the whole investigation. ***
So xHCI/USB power-gating was THE package-C10 blocker (USB host controller keeps
the package out of PC10). Substantial PC10 (goal part 2) = ACHIEVED (52%).
SLP_S0 still 0: platform S0ix needs MORE than PC10. Remaining unmet (mode0,
req & ~newstatus): USB2PLL_OFF/OPIOPLL_OFF(w0=0x50240202), LPSS_PG(w1 bit22),
IS_D3(w2 bit8=0x100), CPU_C10_REQ+others(w4=0x0008a001), wake signals(w5).
NEXT to push PC10 higher + reach SLP_S0: suspend LPSS controllers (I2C/UART/SPI),
the IPU/image-sensor (IS_D3), get USB2PLL/OPIOPLL off (suspend USB hubs/devices
+ the PLL owners), and deepen CPU C10 residency (CPU_C10_REQ). Each cleared
condition -> closer to SLP_S0 -> the slow-blink LED.

* UPDATE 22 (2026-09-18 ~18:20): +LPSS/ISH/audio D3 -> words 1,2,3 met; PC10~49%
Added PMCSR-D3 of driverless IPs (ISH 18:0, LPSS I2C 21:0/21:3, HDaudio 31:3,
TB-DMA 13:2/13:3 @ off 0x84/0x54) to the xHCI+iwx+GPU-D3 freeze.
  RESULT: c7core~99% pc9=0% PC10=49% slp_s0=0
  PMC status 25580074 807c63ef 00001fef 00001040 3d375a2c 000aaaaa
Newly MET vs deep2: LPSS_PG(w1.22 set: 803c->807c), IS_D3(w2.8 set: 1eee->1fef).
Mode0 requirement words 1, 2, 3 now FULLY met (req&~status = 0). PC10 stays ~50%.
REMAINING unmet for mode0 / SLP_S0:
  w0 (clocksource, 0x50240202): USB2PLL_OFF(18), OPIOPLL_OFF(21) + bits 1,9,28,30
     -- PLLs still ON. USB2PLL stays up despite xHCI D3 (shared PLL / USB2 PHY
     not fully off); OPIOPLL tied to some active peripheral I/O. Likely need to
     power down remaining USB2 PHY/PLL owners or the eSPI/SPI/UART LPSS bus.
  w4 (vnn_misc, 0x0008a001): CPU_C10_REQ_STS_0(bit0) + bits 13,15,19 -- CPUs not
     asserting sustained C10 request. Ties to idle-gap/tick depth: PC10=49%
     (cycling) not sustained; sustained all-core C10 needs longer idle gaps
     (stage 3 tick/callout) to set CPU_C10_REQ.
  w5 (signal, 0xaaa00000): Int_Timer_SS_Wake0..5 polarity -- wake-source config
     bits; may be requirement-satisfiable via wake setup, or benign.
STATUS: PC10 substantial (~50%) ACHIEVED. SLP_S0 now blocked by only PLLs (w0),
CPU_C10_REQ/deep-idle (w4), and wake-timer signals (w5) -- most requirement
words cleared. The PLL + CPU_C10_REQ items are the crux for SLP_S0->LED.

* UPDATE 23 (2026-09-18 ~17:30): forceskip REFUTED; live-status is blind; the wall is the PLLs
Built machdep.cidle.forceskip (cidle.c): >0 forces the idle skip (ignoring
callout_next_ticks), still capped by maxskip (<=1400, heartbeat-safe). Goal was
sustained all-core C10 -> assert CPU_C10_REQ_STS_0 (w4.0) + turn off the PLLs.
Live-swapped the module (modunload/modload from /stand) -- no reboot.

NOTE: machine is now on WiFi ONLY (192.168.1.254; USB-eth gone, user relocated).
So iwx0 CANNOT be suspended in a link-attached test (it is the only link).
Tests below keep iwx UP; a self-restoring detached run is needed to add iwx D3.

** forceskip alone (no freeze): inert on the package
tickless=1 forceskip=200 (2s), normal userspace: entries=3688 but deep=0,
ALL woke_early; c7core moved, but pc10 did NOT move at all. Live userspace
interrupts wake every idle attempt within ~13ms -> long gaps never happen
without the freeze. Confirms the freeze (userspace quiesce) is prerequisite.

** STAGE A: full U22 device-D3 set (minus iwx) + forceskip=500 during the hold
  s0freeze done: c7core~99% pc9=0% pc10=46% slp_s0=ok d=0
  PMC status 25580074 807c63ef 00001fef 00001040 3d375a2c 000aaaaa
=> BYTE-FOR-BYTE IDENTICAL to U22 (word4=3d375a2c unchanged). forceskip did
   NOTHING: CPU_C10_REQ_STS_0 still unmet, PLLs still on, pc10 unchanged.
   The "longer idle gaps -> CPU_C10_REQ / SLP_S0" hypothesis is REFUTED.

** WHY forceskip can't work here: the live status is sampled by an AWAKE CPU
CPU_C10_REQ_STS_0 (w4.0) = "this CPU is currently requesting C10". The s0freeze
coordinator reads the LIVE LPM status (0xfe001c5c) right after waking from
kpause -- so the sampling CPU is BY DEFINITION awake and its C10 request is
deasserted. w4.0 can NEVER read satisfied from the sampling CPU. The live-status
method is fundamentally blind to the deepest state. (The LATCHED status at
0x1c3c, captured at LPM entry, is the correct register -- but per-mode residency
at 0x1c80 was ALL ZERO in U20, i.e. the PMC NEVER records entry into any LPM
mode. Ground truth = no LPM entry ever, consistent with slp_s0 counter = 0.)

** STAGE A2: LPS0 ENTRY handshake wrapped around the freeze (was MISSING before)
Discovered the diagnostic s0freeze coordinator NEVER calls acpi_lps0_enter().
The full ENTRY handshake (SCREEN_OFF f3 + MS_ENTRY f7 + ENTRY f5, both UUIDs,
mask-gated: intel=0x7f msft=0x1f9) only runs via hw.acpi.sleep.lps0=1 or the
hw.acpi.sleep.freeze path. Ran: lps0=1 (ENTRY) -> s0freeze -> lps0=0 (EXIT):
  LPS0: Microsoft/Intel _DSM func 5: AE_OK  (ENTRY methods executed)
  s0freeze done: c7core~99% pc9=0% pc10=46% slp_s0=ok d=0
  PMC status 25580074 807c63ef 00001fef 00001040 3d375a2c 000aaaaa (IDENTICAL)
=> The ENTRY notifications execute (AE_OK) but change NOTHING in status/residency.
   S0ix entry is gated by HARDWARE IP requirements, not by the OS notification.

** THE WALL (sample-independent, unmoved by everything tried)
word0 (clocksource) = 0x25580074, unchanged across every test:
  USB2PLL_OFF (bit18) = 0  -> USB2 PHY PLL never off
  OPIOPLL_OFF (bit21) = 0  -> OPIO (display/TypeC I/O) PLL never off
These are NOT sampling artifacts (they don't depend on the CPU being awake).
The PMC turns these PLLs off only when the underlying PHYs are fully powered
down: USB2PLL needs all USB2 activity ceased + PCH USB2 PHY power-gated (xHCI
D3 alone is insufficient -- xHCI_PG/xHCI_D3 are SET but USB2PLL stays on);
OPIOPLL needs the display OPIO fully off = DC9 actually ENTERED (U17: DC9 is
ARMED, DC_STATE_EN bit3=1, but likely never ENTERED -- needs all display power
wells released). NetBSD drives neither PHY power state.

** ASSESSMENT (systematic-debugging Phase 4.5: this is architectural, not a bug)
- Goal 2 (substantial PC10): ACHIEVED, ~46-52%.
- Goal 1 (WiFi/TB in D3, ASPM L1.2): WiFi D3 (drvctl -S iwx0) + TB-DMA D3
  (PMCSR) both demonstrated; link ASPM state still to be re-verified.
- Goal 3 (SLP_S0 / slow-blink LED): BLOCKED at the hardware PLL layer. The PMC
  never enters any LPM mode (residency=0), so SLP_S0# never asserts and the EC
  never slow-blinks. Three independent hypotheses now refuted: (b) GFX0=D3 gate
  (U19), (a) idle-gap length / CPU_C10_REQ via forceskip (U23), and the LPS0
  ENTRY notification (U23). The residual blockers are PHY-level PLL power-gating
  that requires per-IP PM NetBSD does not implement.

** QUESTIONS FOR ASTRA
1. USB2PLL_OFF: with xHCI0/xHCI1 in D3 and xHCI_PG/xHCI0_D3 SET, what still
   holds the PCH USB2 PHY PLL on? Is there a PMC register (USB2 PHY power-gate)
   or an ACPI/_PS3 path that forces it? Any internal USB2 devices (BT/camera/
   fingerprint) on the root hubs keeping USB2 alive that we must D3 first?
2. OPIOPLL_OFF: does this require DC9 to actually ENTER (not just arm)? If so,
   the i915 display-power-domain release (power wells PW1/PW2/combo-PHY,
   CDCLK/DPLL) at suspend_late is the target -- is that worth pursuing on the
   NetBSD i915, or structurally out of reach?
3. Is SLP_S0# reachable AT ALL without full per-IP PHY PM, or should goal 3
   (the LED) be approached via a different mechanism? The live-status method is
   blind (sampling CPU awake); should the coordinator instead read the LATCHED
   status (0x1c3c) + per-mode residency (0x1c80) so we measure real LPM entry?

* UPDATE 24 (2026-09-18 ~17:45): ASPM L1.2 verified; USB2 devices block USB2PLL; iwx-D3 test
** ASPM / L1 PM Substates (goal 1) — VERIFIED via config-space walk
  - NVMe PCIe link (root port dev6): L1SS ext cap @0x200, Ctl1=0x40692d1f ->
    L1.2-ASPM (bit2) = 1. *** ASPM L1.2 ENABLED on the NVMe link. ***
  - TB PCIe root ports (dev7 f0/f2): Link Control=0x0002 (ASPM L1 Entry Enabled);
    no L1SS ext cap exposed at the root port (TB-link L1.2 handled by the TB
    controller firmware, not the PCH root port).
  - WiFi AX201 (dev20 f3): CNVi device -- NOT a PCIe endpoint (no link, no L1SS).
    Classic ASPM L1.2 is N/A; its low-power state is PCI D3 (works via drvctl -S).
  => Goal-1 "link in ASPM L1.2": enabled on all real PCIe links (L1 everywhere,
     L1.2 confirmed on NVMe). WiFi/TB reach D3.

** USB2PLL root cause: three non-detachable active USB2 devices
usbdevs: xhci1 USB2 bus (usb3) has ACTIVE devices: Synaptics fingerprint
(06cb:00bd, ugen0), Chicony Integrated Camera (04f2:b6ea, uvideo0/1), Intel
Bluetooth (8087:0026, ubt0). In U0 (active) the USB2 ports never reach U3, so
the PMC keeps USB2PLL on even with xHCI in D3 (xHCI_PG/xHCI0_D3 SET but
USB2PLL_OFF stays 0). Linux gates USB2PLL via USB autosuspend; NetBSD has none.
STAGE B: drvctl -d uvideo0/uvideo1/ubt0 -> "Device not configured" (marked
non-detachable); only ugen0 detached. Camera+BT stayed -> word0 unchanged.

** STAGE C: full quiesce INCLUDING iwx D3 (detached self-restoring script)
Ran on WiFi-only via a nohup script that suspends iwx (drops ssh), freezes,
then restores iwx + wpa_supplicant/dhcpcd. Reconnected cleanly afterward.
  s0freeze done: c7core~99% pc9=0% pc10=53% slp_s0=ok d=0
  PMC status 25580074 807c63ef 00001fef 00001040 3d175a2c 000aaaaa
=> iwx D3 cleared word4 bit21 (3d375a2c->3d175a2c, a CNVi/WiFi vnn_misc bit) and
   gave the best PC10 (53%). But slp_s0 STILL 0; word0 (PLLs) UNCHANGED.

** EXHAUSTIVE NEGATIVE RESULT for SLP_S0 / the LED (goal 3)
Maximal OS-level quiesce achieved: ALL devices D3 (GPU+RC6-on, xHCI, iwx, TB-DMA,
ISH, LPSS I2C, HDaudio) + LPS0 _DSM ENTRY (AE_OK) + userspace frozen + 99% core
idle + PC10=53%. The PMC STILL never enters any LPM mode (per-mode residency 0,
slp_s0 counter 0). Remaining blockers, now fully attributed:
  - word0 USB2PLL_OFF(b18): blocked by non-autosuspendable USB2 devices
    (camera/BT/fingerprint). Needs USB selective-suspend in the kernel.
  - word0 OPIOPLL_OFF(b21): blocked by display OPIO -- DC9 armed (U17) but not
    ENTERED. Needs i915 display power-well release at suspend_late (DC9 entry).
  - word4 CPU_C10_REQ_STS_0(b0): live-status is blind (sampling CPU awake);
    needs latched-status (0x1c3c) readout to even measure, + sustained all-core
    C10.
All three are real kernel-subsystem development (USB PM, i915 display-power, PMC
latched telemetry) and must ALL land before SLP_S0# asserts -> EC slow-blinks.
This is architectural, not a config/tick tweak. forceskip, GFX0-D3, and the LPS0
notification are all refuted as sufficient.

* UPDATE 25 (2026-09-18 ~18:15): *** C10-LATCHED CAPTURE — OPIOPLL & CPU_C10_REQ were FALSE blockers ***
Implemented Astra's C10-triggered PMC latch in the s0freeze coordinator (kernel
GENERIC #62, acpi.c): before the hold save 0xfe001c34 and clear bit31 (C10
trigger), set ETR3 (0xfe001048) bit28 to clear old capture; after the hold read
the 6 LATCHED words (0xfe001c3c) + 6 LIVE words (0xfe001c5c) separately, check
every access, restore the latch select; also per-mode residency deltas (0x1c80).
This RETRACTS the U24 "three kernel subsystems" conclusion (Astra was right to
push back) and the U24 USB2PLL/OPIOPLL attributions.

Deep quiesce (GPU+RC6 D3, xHCI D3, IP PMCSR-D3, LPS0 ENTRY, iwx UP), pc10=44%:
  PMC live   25580074 807c63ef 00001fef 00001040 3d375a2c 000aaaaa (ok 111111)
  PMC latch  35780074 847de3ef 00001fef 00001040 3d17fa2d 000aaaaa (ok 111111)
  mode resid delta = all 0 (still no LPM entry)

The LATCHED status (captured AT C10 entry) differs from LIVE in words 0,1,4 --
proving the live sample was blind, exactly as diagnosed:
  - w0 bit21 OPIOPLL_OFF: latch=1 (MET at C10). *** OPIOPLL is NOT a blocker;
    the U24 "OPIOPLL needs DC9 entry" hypothesis is REFUTED. ***
  - w4 bit0 CPU_C10_REQ_STS_0: latch=1 (MET at C10). *** CPU_C10_REQ is NOT a
    blocker; forceskip was chasing a live-sample ghost, as suspected. ***
  - w1 (power_gating): latch=847de3ef vs live 807c63ef -- more IPs gated at C10.

Mode-0 requirements vs LATCHED status (req & ~latch) -- the TRUE remaining gate:
  w0 = 0x40040202  -> bits 1, 9, 18(USB2PLL_OFF), 30
  w1 = 0  MET
  w2 = 0  MET
  w3 = 0  MET
  w4 = 0x00280000  -> bits 19, 21
  w5 = 0xaaa00000  -> Int_Timer_SS_Wake0..5_Pol_STS (bits 21,23,25,27,29,31)

** USB2PLL_OFF is an S0ix CONSEQUENCE, not a device/port state
Detached the ENTIRE USB2 hub (uhub3) -> camera+BT+fingerprint all removed
(usbdevs: 0 USB2 devices), xHCI0/1 in D3. Re-ran with C10 latch:
  PMC latch 35780074 847ce3ef ... -> w0 bit18 (USB2PLL_OFF) STILL 0.
So USB2PLL stays on even with ZERO USB2 devices and ports disconnected. It is
NOT held by device U0 state; like SLP_S0 itself it is gated behind actual S0ix
entry. (This also matches Astra's note that netbsd-11 xhci_suspend() already
drives ports to U3 -- the devices were never the issue.)

** Corrected blocker picture (C10-verified)
At real C10 entry, mode-0 requirements are almost met: w1/w2/w3 fully met,
OPIOPLL off, CPU_C10_REQ asserted. The residual unmet-at-C10 bits are narrow:
  - w0 bits 1, 9, 30 (undecoded in the TGL map) + bit18 USB2PLL_OFF (S0ix
    consequence)
  - w4 bits 19, 21 (undecoded; bit21 was the CNVi/WiFi bit, cleared by iwx D3)
  - w5 0xaaa00000 wake-signal polarity (Astra: possibly benign or wake-setup-
    satisfiable)
This is NOT "three subsystems required". It is a small, quantified set of
firmware/PMC-level conditions. NEXT: (1) decode w0 bits 1,9,30 and w4 bits 19,21
against Linux's full TGL/CNP PMC maps (cnp.c/core.c, not just tgl.c); (2)
determine whether w5 wake polarity is configurable or benign; (3) per Astra,
instrument xhci_suspend() per-port PORTSC/U3 readback to confirm the deployed
kernel's sequence (lower priority now that USB2PLL is shown device-independent).
en readback at 0xfe001c34 = 0 (write-only or aliased) but latching demonstrably
works (latch != live, sensible values); etr3 pre-write = 0x81000000.
