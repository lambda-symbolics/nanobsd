--- orig/intel_dp.c	2026-09-26 15:40:42.050226931 +0200
+++ intel_dp.c	2026-09-26 15:40:42.065417101 +0200
@@ -2492,6 +2492,48 @@
 
 	intel_psr_compute_config(intel_dp, pipe_config);
 
+	/*
+	 * LISPBSD: seamless refresh switching (see intel_display_types.h).
+	 * Everything above, the link configuration included, was computed
+	 * for the panel's fixed mode. When userspace asked for the downclock
+	 * mode, keep that link and only lower the pixel clock, which changes
+	 * nothing but the M/N ratio. PSR was decided for the fixed mode too,
+	 * so this cannot turn it on or off.
+	 */
+	if (intel_dp_is_edp(intel_dp) &&
+	    intel_connector->panel.fixed_mode &&
+	    intel_connector->panel.downclock_mode &&
+	    dev_priv->drrs.type == SEAMLESS_DRRS_SUPPORT &&
+	    INTEL_GEN(dev_priv) >= 8 && !IS_CHERRYVIEW(dev_priv) &&
+	    !pipe_config->has_pch_encoder &&
+	    i915_modparams.lispbsd_seamless_rr) {
+		const struct drm_display_mode *fixed =
+			intel_connector->panel.fixed_mode;
+		const struct drm_display_mode *down =
+			intel_connector->panel.downclock_mode;
+		int want = pipe_config->hw.mode.clock;
+		int dfixed = want > fixed->clock ? want - fixed->clock :
+		    fixed->clock - want;
+		int ddown = want > down->clock ? want - down->clock :
+		    down->clock - want;
+
+		pipe_config->seamless_m_n = true;
+		pipe_config->seamless_pixel_clock = fixed->clock;
+
+		if (ddown < dfixed) {
+			intel_link_compute_m_n(output_bpp,
+					       pipe_config->lane_count,
+					       down->clock,
+					       pipe_config->port_clock,
+					       &pipe_config->dp_m_n,
+					       constant_n,
+					       pipe_config->fec_enable);
+			adjusted_mode->clock = down->clock;
+			adjusted_mode->crtc_clock = down->clock;
+			adjusted_mode->vrefresh = down->vrefresh;
+		}
+	}
+
 	return 0;
 }
 
@@ -7433,6 +7475,16 @@
 	}
 
 	intel_panel_init(&intel_connector->panel, fixed_mode, downclock_mode);
+
+	/* LISPBSD: say once what refresh switching this panel allows. */
+	if (fixed_mode && downclock_mode)
+		DRM_INFO("eDP: seamless refresh switching %d/%d Hz "
+		    "(%d/%d kHz, VBT DRRS seamless)\n",
+		    fixed_mode->vrefresh, downclock_mode->vrefresh,
+		    fixed_mode->clock, downclock_mode->clock);
+	else if (fixed_mode)
+		DRM_INFO("eDP: no DRRS downclock mode (VBT DRRS type %d)\n",
+		    dev_priv->vbt.drrs_type);
 	intel_connector->panel.backlight.power = intel_edp_backlight_power;
 	intel_panel_setup_backlight(connector, pipe);
 
--- orig/intel_display.c	2026-09-26 15:40:42.050266365 +0200
+++ intel_display.c	2026-09-26 15:40:42.078049063 +0200
@@ -7832,6 +7832,14 @@
 	pixel_rate = pipe_config->hw.adjusted_mode.crtc_clock;
 
 	/*
+	 * LISPBSD: seamless refresh switching keeps watermarks and cdclk
+	 * sized for the fast mode, so a switch changes neither.
+	 */
+	if (pipe_config->seamless_m_n &&
+	    pipe_config->seamless_pixel_clock > pixel_rate)
+		pixel_rate = pipe_config->seamless_pixel_clock;
+
+	/*
 	 * We only use IF-ID interlacing. If we ever use
 	 * PF-ID we'll need to adjust the pixel_rate here.
 	 */
@@ -13391,6 +13399,13 @@
 	bool fixup_inherited = fastset &&
 		(current_config->hw.mode.private_flags & I915_MODE_FLAG_INHERITED) &&
 		!(pipe_config->hw.mode.private_flags & I915_MODE_FLAG_INHERITED);
+	/*
+	 * LISPBSD: between two seamless states only the pixel clock and the
+	 * M/N ratio may differ; everything that shapes the link and the
+	 * timings is still compared below.
+	 */
+	bool seamless = fastset && current_config->seamless_m_n &&
+		pipe_config->seamless_m_n;
 
 	if (fixup_inherited && !fastboot_enabled(dev_priv)) {
 		DRM_DEBUG_KMS("initial modeset and fastboot not set\n");
@@ -13574,7 +13589,7 @@
 
 		if (current_config->has_drrs)
 			PIPE_CONF_CHECK_M_N(dp_m2_n2);
-	} else
+	} else if (!seamless)
 		PIPE_CONF_CHECK_M_N_ALT(dp_m_n, dp_m2_n2);
 
 	PIPE_CONF_CHECK_X(output_types);
@@ -13645,7 +13660,9 @@
 		}
 
 		PIPE_CONF_CHECK_I(scaler_state.scaler_id);
-		PIPE_CONF_CHECK_CLOCK_FUZZY(pixel_rate);
+		/* LISPBSD: seamless states size pixel_rate for the fast mode */
+		if (!current_config->seamless_m_n)
+			PIPE_CONF_CHECK_CLOCK_FUZZY(pixel_rate);
 
 		PIPE_CONF_CHECK_X(gamma_mode);
 		if (IS_CHERRYVIEW(dev_priv))
@@ -13702,7 +13719,8 @@
 	if (IS_G4X(dev_priv) || INTEL_GEN(dev_priv) >= 5)
 		PIPE_CONF_CHECK_I(pipe_bpp);
 
-	PIPE_CONF_CHECK_CLOCK_FUZZY(hw.adjusted_mode.crtc_clock);
+	if (!seamless)
+		PIPE_CONF_CHECK_CLOCK_FUZZY(hw.adjusted_mode.crtc_clock);
 	PIPE_CONF_CHECK_CLOCK_FUZZY(port_clock);
 
 	PIPE_CONF_CHECK_I(min_voltage_level);
@@ -14361,6 +14379,12 @@
 
 	new_crtc_state->uapi.mode_changed = false;
 	new_crtc_state->update_pipe = true;
+
+	/* LISPBSD: a seamless refresh switch rewrites the link M/N. */
+	if (old_crtc_state->seamless_m_n && new_crtc_state->seamless_m_n &&
+	    !intel_fuzzy_clock_check(old_crtc_state->hw.adjusted_mode.crtc_clock,
+				     new_crtc_state->hw.adjusted_mode.crtc_clock))
+		new_crtc_state->update_m_n = true;
 }
 
 static void intel_crtc_copy_fastset(const struct intel_crtc_state *old_crtc_state,
@@ -14375,8 +14399,11 @@
 	 * FIXME: should really copy more fuzzy state here
 	 */
 	new_crtc_state->fdi_m_n = old_crtc_state->fdi_m_n;
-	new_crtc_state->dp_m_n = old_crtc_state->dp_m_n;
-	new_crtc_state->dp_m2_n2 = old_crtc_state->dp_m2_n2;
+	/* LISPBSD: unless this fastset switches the refresh seamlessly */
+	if (!new_crtc_state->update_m_n) {
+		new_crtc_state->dp_m_n = old_crtc_state->dp_m_n;
+		new_crtc_state->dp_m2_n2 = old_crtc_state->dp_m2_n2;
+	}
 	new_crtc_state->has_drrs = old_crtc_state->has_drrs;
 }
 
@@ -14819,6 +14846,23 @@
 
 	if (INTEL_GEN(dev_priv) >= 11)
 		icl_set_pipe_chicken(crtc);
+
+	/*
+	 * LISPBSD: seamless refresh switch. We are inside vblank evasion and
+	 * the M/N registers are double buffered, so the new ratio latches at
+	 * the next start of vblank with the rest of this update. Move the
+	 * vblank timestamping constants to the new pixel clock with it.
+	 */
+	if (new_crtc_state->update_m_n) {
+		unsigned long irqflags;
+
+		intel_cpu_transcoder_set_m_n(new_crtc_state,
+					     &new_crtc_state->dp_m_n, NULL);
+
+		spin_lock_irqsave(&dev_priv->drm.vblank_time_lock, irqflags);
+		intel_crtc_update_active_timings(new_crtc_state);
+		spin_unlock_irqrestore(&dev_priv->drm.vblank_time_lock, irqflags);
+	}
 }
 
 static void commit_pipe_config(struct intel_atomic_state *state,
--- orig/intel_ddi.c	2026-09-26 15:40:42.050350124 +0200
+++ intel_ddi.c	2026-09-26 15:40:42.078231513 +0200
@@ -4092,6 +4092,12 @@
 	intel_ddi_set_dp_msa(crtc_state, conn_state);
 
 	intel_psr_update(intel_dp, crtc_state);
+	/*
+	 * LISPBSD: a seamless refresh switch changes the M/N values idleness
+	 * DRRS alternates between; restart DRRS from the new state.
+	 */
+	if (crtc_state->update_m_n)
+		intel_edp_drrs_disable(intel_dp, crtc_state);
 	intel_edp_drrs_enable(intel_dp, crtc_state);
 
 	intel_panel_update_backlight(encoder, crtc_state, conn_state);
--- orig/intel_atomic.c	2026-09-26 15:40:42.050379177 +0200
+++ intel_atomic.c	2026-09-26 15:40:42.078471232 +0200
@@ -243,6 +243,7 @@
 		drm_property_blob_get(crtc_state->hw.gamma_lut);
 
 	crtc_state->update_pipe = false;
+	crtc_state->update_m_n = false;		/* LISPBSD */
 	crtc_state->disable_lp_wm = false;
 	crtc_state->disable_cxsr = false;
 	crtc_state->update_wm_pre = false;
--- orig/intel_display_types.h	2026-09-26 15:40:42.050184691 +0200
+++ intel_display_types.h	2026-09-26 15:40:42.064108820 +0200
@@ -926,6 +926,20 @@
 	struct intel_link_m_n dp_m2_n2;
 	bool has_drrs;
 
+	/*
+	 * LISPBSD: seamless refresh switching, a port of Linux 6.x seamless
+	 * M/N. The eDP panel's fixed and downclock modes share every timing
+	 * except the pixel clock, so the refresh changes by rewriting the
+	 * double-buffered link M/N values in a fastset instead of a modeset.
+	 * seamless_m_n: this state may switch that way.
+	 * seamless_pixel_clock: the clock watermarks and cdclk are sized for
+	 *	(the fast mode's), so they stay put across a switch.
+	 * update_m_n: this fastset must write dp_m_n.
+	 */
+	bool seamless_m_n;
+	bool update_m_n;
+	int seamless_pixel_clock;
+
 	bool has_psr;
 	bool has_psr2;
 	u32 dc3co_exitline;
--- orig/i915_params.h	2026-09-26 15:40:42.050210051 +0200
+++ i915_params.h	2026-09-26 15:40:42.064152948 +0200
@@ -53,6 +53,7 @@
 	param(int, enable_dc, -1) \
 	param(int, enable_fbc, -1) \
 	param(int, enable_psr, -1) \
+	param(int, lispbsd_seamless_rr, 1) \
 	param(int, disable_power_well, -1) \
 	param(int, enable_ips, 1) \
 	param(int, invert_brightness, 0) \
--- orig/i915_params.c	2026-09-26 15:40:42.050218239 +0200
+++ i915_params.c	2026-09-26 15:40:42.064198093 +0200
@@ -93,6 +93,11 @@
 	"(0=disabled, 1=enabled) "
 	"Default: -1 (use per-chip default)");
 
+i915_param_named_unsafe(lispbsd_seamless_rr, int, 0600,
+	"Switch eDP between the panel's fixed and downclock (DRRS) modes "
+	"without a modeset by rewriting the link M/N values "
+	"(0=off, 1=on [default])");
+
 i915_param_named_unsafe(force_probe, charp, 0400,
 	"Force probe the driver for specified devices. "
 	"See CONFIG_DRM_I915_FORCE_PROBE for details.");
